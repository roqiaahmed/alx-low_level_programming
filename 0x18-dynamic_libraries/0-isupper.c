#include "main.h"
#include <ctype.h>
/**
 * _isupper - checks for uppercase character
 * @c: The character to print
 *
 * Return: On success 1. 0 otherwise
 */
int _isupper(int c)
{
if (isupper(c))
return (1);
else
return (0);
}
